<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reimposta Password - Stook</title>

  <link href="https://fonts.googleapis.com/css2?family=Luckiest+Guy&display=swap" rel="stylesheet">

  <style>
    :root {
      --text: #000000;
      --bg: #ffffff;
      --card: #f8f8f8;
      --muted: #666;
      --border: #e6e6e6;
      --success-bg: #f1f7f1;
      --success-border: #cfe3cf;
      --error-bg: #fff2f2;
      --error-border: #ecc9c9;
      --error-text: #7a1f1f;
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      min-height: 100vh;
      font-family: 'Inter', sans-serif;
      background: var(--bg);
      color: var(--text);
    }

    .topbar {
      width: 100%;
      padding: 14px 16px;
    }

    .brand {
      display: inline-block;
      font-family: 'Luckiest Guy', cursive;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-size: 1.5rem;
      line-height: 1;
      color: var(--text);
      text-decoration: none;
    }

    main {
      min-height: calc(100vh - 58px);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 24px 20px 48px;
    }

    .card {
      width: min(100%, 560px);
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 24px;
      padding: 38px 30px 32px;
      text-align: left;
    }

    h1 {
      margin: 0 0 12px;
      font-size: clamp(2rem, 4vw, 3rem);
      text-align: center;
    }

    .intro {
      margin: 0 0 28px;
      color: var(--muted);
      text-align: center;
      font-size: 1rem;
      line-height: 1.6;
    }

    .field {
      margin-bottom: 18px;
    }

    label {
      display: block;
      margin-bottom: 8px;
      font-weight: 700;
      font-size: 0.96rem;
    }

    input {
      width: 100%;
      padding: 14px 16px;
      border: 1px solid #d3d3d3;
      border-radius: 14px;
      font-size: 1rem;
      background: #ffffff;
      color: var(--text);
      outline: none;
      transition: border-color 0.2s ease, box-shadow 0.2s ease;
    }

    input:focus {
      border-color: #000000;
      box-shadow: 0 0 0 4px rgba(0, 0, 0, 0.08);
    }

    .cta-btn {
      display: inline-block;
      width: 100%;
      background: #000000;
      color: #ffffff;
      padding: 14px 28px;
      border-radius: 14px;
      text-decoration: none;
      font-weight: 700;
      font-size: 1rem;
      text-align: center;
      transition: transform 0.08s ease, background 0.3s ease;
      cursor: pointer;
      border: none;
      margin-top: 8px;
    }

    .cta-btn:hover {
      background: #333333;
      transform: translateY(-1px);
    }

    .message {
      margin-top: 18px;
      padding: 16px 18px;
      border-radius: 16px;
      line-height: 1.6;
      font-size: 0.95rem;
      display: none;
    }

    .message.success {
      background: var(--success-bg);
      border: 1px solid var(--success-border);
      color: #234023;
    }

    .message.error {
      background: var(--error-bg);
      border: 1px solid var(--error-border);
      color: var(--error-text);
    }
    .message.info {
      background: #f5f5f5;
      border: 1px solid #dddddd;
      color: #333333;
      display: block;
    }

    .back-link {
      display: inline-block;
      margin-top: 18px;
      color: var(--text);
      text-decoration: none;
      font-weight: 600;
    }

    .back-link:hover {
      text-decoration: underline;
    }

    @media (max-width: 520px) {
      .topbar {
        padding: 12px 14px;
      }

      .card {
        padding: 30px 20px 24px;
        border-radius: 20px;
      }
    }
  </style>
</head>
<body>
  <div class="topbar">
    <a href="index.html" class="brand">STOOK</a>
  </div>

  <main>
    <section class="card">
      <h1>Reimposta password</h1>
      <p class="intro">Conferma la tua email e scegli una nuova password per aggiornare l'accesso al tuo profilo Stook.</p>

      <form id="reset-form" method="post" action="#" autocomplete="on">
        <div class="field">
          <label for="reset-email">Email</label>
          <input type="email" id="reset-email" name="email" placeholder="nome@email.com" autocomplete="email" inputmode="email" required>
        </div>

        <div class="field">
          <label for="new-password">Nuova password</label>
          <input type="password" id="new-password" name="new-password" placeholder="Crea una nuova password" autocomplete="new-password" required>
        </div>

        <div class="field">
          <label for="confirm-password">Conferma nuova password</label>
          <input type="password" id="confirm-password" name="confirm-password" placeholder="Riscrivi la nuova password" autocomplete="new-password" required>
        </div>

        <button type="submit" class="cta-btn">Salva nuova password</button>
      </form>

      <div id="reset-info" class="message info">Apri questa pagina dal link ricevuto via email per autorizzare il cambio password.</div>
      <div id="reset-success" class="message success"></div>
      <div id="reset-error" class="message error"></div>

      <a href="accedi.html" class="back-link">Torna ad accedi</a>
    </section>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script>
    const SUPABASE_URL = 'https://uaelkranbqialxjpoqkk.supabase.co';
    const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InVhZWxrcmFuYnFpYWx4anBvcWtrIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzYxNzExMDcsImV4cCI6MjA5MTc0NzEwN30.3RAuhcrAlemr9EpwQsdJRj6xzeS2CH0OxRvQrHVFdns';
    const supabaseClient = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
    const params = new URLSearchParams(window.location.search);
    const resetEmailInput = document.getElementById('reset-email');
    const newPasswordInput = document.getElementById('new-password');
    const confirmPasswordInput = document.getElementById('confirm-password');
    const resetForm = document.getElementById('reset-form');
    const resetInfo = document.getElementById('reset-info');
    const resetSuccess = document.getElementById('reset-success');
    const resetError = document.getElementById('reset-error');
    const emailFromLink = params.get('email');

    if (emailFromLink) {
      resetEmailInput.value = emailFromLink;
    }

    async function initializeRecoveryState() {
      const { data } = await supabaseClient.auth.getSession();
      const sessionEmail = data.session && data.session.user ? data.session.user.email : '';

      if (sessionEmail) {
        resetEmailInput.value = sessionEmail;
        resetInfo.textContent = 'Link verificato. Conferma la tua email e scegli una nuova password.';
      } else {
        resetInfo.textContent = 'Apri questa pagina dal link ricevuto via email per autorizzare il cambio password.';
      }
    }

    initializeRecoveryState();

    resetForm.addEventListener('submit', async function (event) {
      event.preventDefault();

      resetSuccess.style.display = 'none';
      resetError.style.display = 'none';

      const email = resetEmailInput.value.trim();
      const newPassword = newPasswordInput.value;
      const confirmPassword = confirmPasswordInput.value;
      const { data } = await supabaseClient.auth.getSession();
      const sessionEmail = data.session && data.session.user ? data.session.user.email : '';

      if (!sessionEmail) {
        resetError.textContent = 'Sessione di recupero non valida o scaduta. Richiedi un nuovo link dalla pagina di recupero password.';
        resetError.style.display = 'block';
        return;
      }

      if (email.toLowerCase() !== sessionEmail.toLowerCase()) {
        resetError.textContent = 'L\\'email inserita non corrisponde a quella del link di recupero. Controlla e riprova.';
        resetError.style.display = 'block';
        return;
      }

      if (newPassword !== confirmPassword) {
        resetError.textContent = 'Le password non coincidono. Controlla i campi e riprova.';
        resetError.style.display = 'block';
        return;
      }

      const { error } = await supabaseClient.auth.updateUser({
        password: newPassword
      });

      if (error) {
        resetError.textContent = error.message;
        resetError.style.display = 'block';
        return;
      }

      resetSuccess.innerHTML = 'Password aggiornata con successo per <strong>' + email + '</strong>.<br><br>Ora puoi tornare alla pagina di accesso e usare le nuove credenziali.';
      resetSuccess.style.display = 'block';
      resetForm.reset();

      resetEmailInput.value = email;
    });
  </script>
</body>
</html>
